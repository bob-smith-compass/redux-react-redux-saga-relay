import React from 'react';

const url = 'https://restcountries.eu/rest/v2/all';

const RESTCall = () => {
    const getData = (url) => {

    }
    return (
        <div>
            {url}
        </div>
    );
}

export default RESTCall;